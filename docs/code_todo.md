[birkhoff_polytope.html](file;file:///d%3A/PythonProject/llm_learning/notebooks/.assets/birkhoff_polytope.html)   可编辑矩阵（3×3） 双随机矩阵的约束示意 这部分里面矩阵的数字颜色太浅了，
双随机矩阵的约束示意
右边还有下方的字显示不全，（“1.0”  所有元素》=0这些）

“B(t) = [[t, 1-t], [1-t, t]]，t ∈ [0, 1]"这部分的背景依然是黑色的

两个端点 t=0 和 t=1 恰好是两个置换矩阵。拖动滑块，观察矩阵如何在两个置换矩阵之间插值。这里的字看不清，B矩阵背景是黑色的，所有字都看不清


这一节涉及三个"跳级"概念：置换矩阵、多面体的顶点、4D → 2D 投影。我们一步步拆开来看。
这里的示意图背景依然是黑色的

③.1 什么是置换矩阵？  ③.3 把 6 个点"包"成多面体，再投影到 2D
④ 任意双随机矩阵 = 置换矩阵的凸组合
⑤ 为什么约束到 Birkhoff polytope → 训练更稳定
⑥.2 实时迭代 demo（4×4 矩阵）

这几个地方的内部示意图背景依然是黑色的


xl+1 = Hres · xl + Hpost ⊙ F( Hpre · xl )
这段内容的背景依然是黑色的


下面是简化的 mHC 残差演化（聚焦 H_res 这一步）：
• ① 上方 4 条彩色横条 = 当前 4 个残差流 x_l
• ② 中间 4×4 矩阵 = H_res（每格可编辑，或点下方预设按钮）
• ③ 下方 4 条横条 = H_res · x_l，即下一层的残差流
• 关键观察：当 H_res 是双随机时 → 4 条流的总能量（数值之和）保持不变（谱范数 ≤ 1 的体现） 这里x_l 、H_res · x_l 背景是黑色的


预设：单位矩阵
预设：均匀混合
预设：循环移位
Sinkhorn 归一化
▶ 应用 H_res 到流
重置流
这几个按钮是黑色的

修复上面残余的颜色不统一问题


